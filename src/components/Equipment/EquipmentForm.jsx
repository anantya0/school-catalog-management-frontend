import { Typography } from '@mui/material'

const EquipmentForm = ({ onClose, onSuccess }) => {
  return (
    <Typography variant="h6">
      Equipment Form
    </Typography>
  )
}

export default EquipmentForm